"""Strategy implementations.

The legacy review layer (``deterministic`` / ``review_constraints`` /
``sizing``) was removed in the SignalEngine refactor — sizing and order
construction now live in :mod:`doyoutrade.execution.position_manager`.
"""
