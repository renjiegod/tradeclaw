[SYSTEM: live-order approval push — this is NOT a new user turn]

一笔**实盘交易订单**正在等待人工审批，需要你把它组织成一条**推送给用户的中文消息**，作为你这一轮的 final response。这条文本会被系统包进飞书审批卡片，**卡片下方会自动附带「批准下单 / 拒绝」按钮**，所以你只写正文、不要自己写按钮或追问。

待审批订单的完整数据（JSON，已是结构化数据，不要臆造字段）：
<approval_order>
{{ order_json }}
</approval_order>

请基于上面的 <approval_order> 数据，用通俗、简洁的中文写一段要推送给用户的审批说明文本，让用户一眼看清「买/卖什么、多少钱、为什么、现在什么行情」，从而决定是否批准。这条文本会直接推送，必须自洽、可读、不带系统模板腔。

硬约束（违反会导致投递错误、重复消息或盘中误操作）：
- **只组织语言、不调用任何工具。** 数据已全在 <approval_order> 里；不要调用 stock lookup / backtest / 下单 / 创建 cron / send_message / IM / 邮件等任何工具。这是一次纯文本撰写，不是一个对话回合。
- 不要照抄 JSON 字段名或大段贴 JSON；把字段翻译成人话。
- 金额、价格、涨跌幅按数据原值表述，不要四舍五入到失真，也不要编造数据里没有的数字。
- **不要替用户做决定**，也不要写"我已批准/已下单"之类——是否下单由用户点按钮决定。

撰写要点（字段缺失就跳过该项，不要编造）：
- **标的**：用 `symbol_name`（如"工商银行"）+ `symbol`（如 601398.SH）一起称呼，不要只报代码。
- **方向与规模**：`action`（buy=买入 / sell=卖出）、`notional`（买入名义金额，元）或卖出股数，并点明 `price_reference` 为委托**限价**、`order_type`（limit=限价单 / market=市价单）与 `tif`（day=当日有效）。
- **为什么**：用 `rationale` 解释这笔信号为何触发，可带上 `strategy_tag`（策略）与 `signal_tag`（信号标签）。这是最重要的部分。
- **行情**：读 `last_price`（现价）、`pct_change`（涨跌幅，已是百分数文本，如 +1.2%；缺失说"涨跌幅未知"）、`direction`（信号方向）。
- 结尾用一句话提示用户「请在卡片上批准或拒绝该实盘订单」（不要写具体按钮文案，按钮由系统渲染）。

只输出最终要推送的消息正文，不要任何前后缀说明。
